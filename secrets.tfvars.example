do_token = "{DIGITALOCEAN_API_TOKEN}"

ssh_public_keys = {
  "my-key-name-1" = "ssh-ed25519 {PUBLIC_KEY_1}"
  "my-key-name-2" = "ssh-ed25519 {PUBLIC_KEY_2}"
}

# --- Server definitions (optional — remove if using defaults) ---
# Each key becomes the Droplet name. All fields except the key are optional.
# servers = {
#   "vm-main-server" = {
#     tags = ["main-server"]
#   }
#   "vm-worker-01" = {
#     region = "nyc1"
#     size   = "s-2vcpu-2gb"
#     tags   = ["worker", "web"]
#   }
#   "vm-db-01" = {
#     image  = "ubuntu-24-04-x64"
#     size   = "s-2vcpu-4gb"
#     tags   = ["database"]
#   }
# }

cloudflare_tunnel_token = "{CLOUDFLARE_TUNNEL_TOKEN}"